language: python
python:
  - "2.7"
install:
  - pip install flake8
  - pip install "django>=1.5,<1.6"
  - pip install mysql-python
script:
  - python manage.py test --settings=pursuite.settings.testing
  - flake8 .
